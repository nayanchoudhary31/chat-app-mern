const asyncHandler = require('express-async-handler')

const userRegister = asyncHandler(async () => {})

const loginUserAuth = asyncHandler(async () => {})

module.exports = { userRegister, loginUserAuth }
